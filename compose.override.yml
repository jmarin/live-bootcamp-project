services:
  app-service:
    build:
      context: ./app-service # specify directory where local Dockerfile is located
  auth-service:
    build:
      context: . # use project root so test_helpers path dep is accessible
      dockerfile: auth-service/Dockerfile